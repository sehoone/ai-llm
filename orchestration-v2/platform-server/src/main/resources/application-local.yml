auth:
  mode: jwt  # 로컬: 공유 비밀키 HS256

spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.sehoon.platform: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG
