apiVersion: v1
kind: Service
metadata:
  name: langfuse
  namespace: llm-platform
  labels:
    app.kubernetes.io/name: langfuse
    app.kubernetes.io/part-of: llm-platform
spec:
  selector:
    app.kubernetes.io/name: langfuse
  ports:
    - name: http
      port: 3000
      targetPort: 3000
