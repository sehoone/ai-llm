apiVersion: v1
kind: Service
metadata:
  name: platform
  namespace: llm-platform
  labels:
    app.kubernetes.io/name: platform
    app.kubernetes.io/part-of: llm-platform
spec:
  selector:
    app.kubernetes.io/name: platform
  ports:
    - name: http
      port: 8080
      targetPort: 8080
