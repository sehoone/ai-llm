apiVersion: v1
kind: Service
metadata:
  name: admin-front
  namespace: llm-platform
  labels:
    app.kubernetes.io/name: admin-front
    app.kubernetes.io/part-of: llm-platform
spec:
  selector:
    app.kubernetes.io/name: admin-front
  ports:
    - name: http
      port: 3000
      targetPort: 3000
