# 비워두면 axios가 '/api' (상대경로) 사용 → 브라우저가 Nginx(/api/*)로 요청
NEXT_PUBLIC_API_URL=
# Nginx를 통해 WebSocket 연결 (http→ws, Nginx가 orchestration으로 프록시)
NEXT_PUBLIC_WS_URL=ws://{서버ip}:8060/api/v1/voice-evaluation/ws/conversation
