version: '3.8'

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: poc-pgvector
    environment:
      POSTGRES_DB: poc_vector
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgvector_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d poc_vector"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgvector_data:
