# 브라우저 → Nginx → vector-server 구조이므로 NEXT_PUBLIC_API_URL은 비워둠
# 비어있으면 axios baseURL이 /api/ (상대경로) → 브라우저가 같은 호스트로 요청
NEXT_PUBLIC_API_URL=
# SSR 리다이렉트용 (Next.js 서버 → vector-server 직접)
API_URL=http://vector-server:8080
